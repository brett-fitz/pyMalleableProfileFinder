import asyncio
from malleablec2 import Profile


class Compare:

    @staticmethod
    async def compare_beacon_config(
            beacon_config: str,
            malleable_profile: str
    ):
        return
